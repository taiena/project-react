import React from "react";
import classes from "./Users.module.scss";

const Users = (props) => {
  return (
    <div className={classes.findUsers}>
      <span>Find users page</span>
    </div>
  );
};

export default Users;
